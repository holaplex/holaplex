query whoToFollow($limit: Int = 25, $offset: Int = 0) {
  followWallets(limit: $limit, offset: $offset) {
    __typename
    profile {
      handle
      profileImageUrl
    }
    address
    bids {
      lastBidTime
      listing {
        nfts {
          address
          name
          image
        }
      }
    }
    connectionCounts {
      fromCount
      toCount
    }
    nftCounts {
      owned
      offered
      listed
    }
  }
}
