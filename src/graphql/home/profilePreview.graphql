query profilePreview($address: PublicKey!) {
  wallet(address: $address) {
    profile {
      handle
      profileImageUrl
      bannerImageUrl
    }
    address
    nftCounts {
      owned
      created
    }
  }
}