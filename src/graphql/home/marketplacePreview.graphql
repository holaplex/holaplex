query marketplacePreview($subdomain: String!) {
  marketplace(subdomain: $subdomain) {
    subdomain
    name
    bannerUrl
    creators {
      creatorAddress
    }
    auctionHouse {
      stats {
        floor
      }
    }
    stats {
      nfts
    }
  }
}
