query marketplacePreview($subdomain: String!) {
  marketplace(subdomain: $subdomain) {
    subdomain
    bannerUrl
    stats {
      nfts
    }
    name
    auctionHouse {
      stats {
        floor
      }
    }
    creators {
      creatorAddress
    }
  }
}