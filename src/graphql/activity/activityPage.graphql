query activityPage($address: String!) {
  wallet(address: $address) {
    bids {
      __typename
      listingAddress
      bidderAddress
      lastBidTime
      cancelled
      listing {
        address
        storeOwner
        ended
        storefront {
          __typename
          ownerAddress
          subdomain
          title
          description
          faviconUrl
          logoUrl
          bannerUrl
        }
        nfts {
          __typename
          address
          name
          description
          image
        }
      }
    }
  }
}
