# import "../fragments/NftCard.graphql"

query discoverCollectionsByVolume($searchTerm: String, $start: DateTimeUtc!, $end: DateTimeUtc!, $limit: Int!, $offset: Int!) {
  collectionsFeaturedByVolume(term:$searchTerm, startDate:$start endDate:$end, limit: $limit, offset:$offset, orderDirection:DESC) {
    ...NftCard
  }
  marketplace(subdomain: "haus") {
    ...MarketplaceAuctionHouse
  }
}
