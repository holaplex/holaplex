import { CSSObject } from 'styled-components';

const contrast = {
  primary: {
    color: '#fff',
  } as CSSObject,
  secondary: {
    color: 'rgba(253, 253, 253, 0.6)',
  } as CSSObject,
};

export default {
  contrast,
};
