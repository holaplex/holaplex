schema: ${NEXT_PUBLIC_HASURA_GRAPHQL_URL}
documents: './src/graphql/graphql-engine/**/*.graphql'
generates:
  ./src/graphql/gqlEngineTypes.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
config:
  withHOC: false
  withComponent: false
  withHooks: true
